import React from 'react';
import Layout from '../components/shared/layout/Layout';

const EmptyScreen: React.FC = () => {
  return (
    <Layout>Empty Content</Layout>
  );
};

export default EmptyScreen;
